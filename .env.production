# Public Turnstile site key — embedded at build time (safe to commit; not a secret).
# Supabase/anon keys stay in Vercel env or .env.local; this file only ensures CAPTCHA
# is present when VITE_* vars are not set in the hosting dashboard.
VITE_TURNSTILE_SITE_KEY=0x4AAAAAAC1hocPw8c-hqea_
