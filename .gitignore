# Dependencies
node_modules

# Build
dist

# Environment (contains secrets — never commit)
.env
.env.local
.env.*.local
.env.production
.env.development
.env.test
.env.staging
# Allow the documented template only
!.env.example

# IDE
.vscode/*
!.vscode/extensions.json
.idea

# OS
.DS_Store
*.log
# Vercel CLI cache (keep project link in repo — see .vercel/project.json)
.vercel/*
!.vercel/project.json

# Supabase CLI — local cache
supabase/.temp

# Local design references (Figma / Claude design canvas exports)
design/
